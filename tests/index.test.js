const index = require("../index");

describe('The populatron', () => {
  it('should print the world population', () => {
    //your test goes here
    
  })
});

/*
const sum = require("./sum.js");


describe("sum suite", function () {
    // Define the individual tests
    test ("should add 2 positive numbers together and return the result", function() {
        // This is the test assertion: basically a way of expressing expectations on how something should behave
        expect(sum(1,2)).toBe(3); //toBe is called a "matcher"
    });*/